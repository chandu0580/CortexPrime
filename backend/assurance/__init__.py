"""The Assurance Plane — an independent evaluator of reasoning claims.

WORLD -> INTELLIGENCE -> ASSURANCE -> HARNESS -> GOVERNANCE -> EXECUTION.

Assurance evaluates claims against evidence it obtains *independently* from the
World Plane. It is not an LLM, an execution engine, a connector, a gateway, a
scheduler, or a planner. It cannot execute. It is the only sanctioned minter of a
WorldVerification, and it never accepts a model's own assertion as evidence.
"""
